import styled from 'styled-components';

export const EditWalletContainer = styled.div`
  position: relative;
  height: 100%;
  width: 100%;
  border-radius: 20px;

  & .ui.page.modals {
    background-color: rgba(0, 0, 0, 0.3);
    border-radius: 20px;
  }
`;
